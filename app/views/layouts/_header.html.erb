<nav class="navbar mb-2">
  <div class="container-fluid">
    <div class="navbar-header">
    <%= link_to image_tag("logos/logo.png", class: 'logo'), root_path %>
    </div>

    <div class="navbar-header">
    <%= link_to "Home", root_path, class: "mx-2" %>
    
    
      <% categories().each do | category | %>
        <%= link_to category.name, category_articles_path(category.id), class: "mx-2" %>
      <% end %>
    
      

    <% if logged_in? %>
    <%= link_to  "Create an article", new_article_path %>
    <% end %>
  </div> 

  
    <% if logged_in? %>
     
    <div class="navbar-header search">
    <%= form_tag 'search', {method: :get, class: 'col-12 col-lg-7 position-relative'}  do %>
      <%= text_field_tag 'article[title]', '', class: 'form-control' %>
      <button type = 'submit' class = 'search position-absolute'> 
         <i class="fa fa-search text-gray" style="font-size:20px">
      </i>
    <% end %>
    </div>

      <ul class="nav navbar-right">
        <li><%= link_to "Log out", logout_path, class: "mx-2", method: :delete %></li> 
      </ul>

      
      
    <% else %>
      <ul class="nav navbar-right ">
        <li><%= link_to "Log in", login_path, class: "mx-2" %></li> 
        <li><%= link_to "Sign up", signup_path, class: "mx-2" %></li>
      </ul>
    <% end %>
  </div>
</nav>