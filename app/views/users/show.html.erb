<div class="card w-50 mx-auto mt-5">
  <div class="card-header">
    <h1><%= @user.name %></h1>
  </div>
  <div class="card-body">
    <p class="card-text"><%= @user.username %></p>
    <% if !session[:current_user].nil? && session[:current_user]['id'] == user.id %>
      <%= link_to  "Edit", edit_user_path(user) %>
    <% end %>
  </div>
</div>

<div class = 'p-3 w-50 bg-white mx-auto'><%= link_to  "Back to all users", users_path %></div>
<div class = 'p-3 w-50 bg-white mx-auto'><%= link_to  "Create an article", new_article_path %></div>
<div class = 'p-3 w-50 bg-white mx-auto'>
<div>

